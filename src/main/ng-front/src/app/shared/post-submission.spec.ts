import { PostSubmission } from './post-submission';

describe('PostSubmission', () => {
  it('should create an instance', () => {
    expect(new PostSubmission("sdl;kdl;sd", "kljfklsfj")).toBeTruthy();
  });
});
